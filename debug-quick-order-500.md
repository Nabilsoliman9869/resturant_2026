# [OPEN] Debug Session: quick-order-500

## Symptom
- في لوحة الصالة/الطلب السريع يظهر: `فشل الإرسال: Error: Internal Server Error`.

## Expected
- إرسال الطلب ينجح أو يعرض سببًا تشغيليًا واضحًا بدل `500`.

## Reproduction
1. فتح `لوحة الصالة`.
2. اختيار طاولة مثل `T10`.
3. إضافة صنف ثم تنفيذ الإرسال.
4. مراقبة الطلب الشبكي والرد من الباك إند.

## Hypotheses
- H1: endpoint إرسال الطلب يرجع `500` بسبب حقل ناقص أو غير متوقع في payload القادم من الواجهة.
- H2: توجد جلسة/طاولة بحالة غير متطابقة تجعل مسار الإرسال يفشل داخليًا عند الربط مع session أو table.
- H3: منطق تحديث الطلب/المطبخ أو كتابة الملفات المحلية يفشل بعد قبول الطلب مبدئيًا.
- H4: الـ quick order يستخدم مسارًا مختلفًا عن الطلب العادي، وفيه regression حديث لا يظهر في بقية المسارات.
- H5: الخطأ ليس من business logic نفسه بل من استثناء runtime غير ملتقط في الباك إند أثناء معالجة `T10`.

## Evidence Plan
- التقاط request/response الفعلي من المتصفح.
- تحديد endpoint الذي يرجع `500` والـ payload المرسل.
- مطابقة ذلك مع سجل/trace الباك إند لنفس اللحظة.
- بعد تعيين السبب المؤكد فقط يتم تنفيذ إصلاح محصور.
