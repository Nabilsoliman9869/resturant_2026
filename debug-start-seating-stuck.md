# [OPEN] Debug Session: start-seating-stuck

## Symptom
- في بطاقة `T12` يبقى النص `جاري التسكين...` ويستمر شعور البطء، مع بقاء الحالة `مشغولة`.

## Expected
- عند نجاح `ابدأ التسكين` يجب أن تنتقل البطاقة فورًا إلى حالة مستقرة مثل `أنت الكابتن` أو `الطاولة مسندة` دون بقاء نص التحميل.

## Reproduction
1. فتح شاشة الطاولات المحلية.
2. الضغط على `ابدأ التسكين` أو الوصول إلى طاولة مثل `T12`.
3. مراقبة نص الزر وحالة البطاقة والزمن حتى الاستقرار.

## Hypotheses
- H1: طلب `claimCaptain` لا ينتهي أصلًا أحيانًا، فيبقى `claimBusyTableId` غير مُصفّر.
- H2: يتم إطلاق أكثر من طلب متداخل لنفس الطاولة بسبب polling أو إعادة تحميل، فتُكتب حالة قديمة فوق الحالة الجديدة.
- H3: `sessionByTable` أو `sessions` لا تُحدَّثان بالقيمة النهائية الصحيحة بعد الرد، فيستمر الزر في مسار وسيط.
- H4: بطاقة `T12` تعرض جلسة نشطة قديمة أو غير متطابقة مع `tableId` الحالي، فتبدو `مشغولة` مع بقاء سلوك التسكين معلقًا.
- H5: بطء API أو تعارضه مع polling يجعل الواجهة تعرض حالة نجاح جزئي ثم تعود إلى حالة غير مستقرة.

## Evidence Plan
- قياس استجابة `table-sessions`, `tables`, و`operational-snapshot`.
- إعادة إنتاج الحالة على `T12` مع مراقبة استجابة API وبيانات الجلسة للطاولة.
- بعد تحديد الفرضية الصحيحة فقط يتم تنفيذ إصلاح محصور.
