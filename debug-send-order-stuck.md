# [OPEN] Debug Session: send-order-stuck

## Symptom
- عند إرسال طلب لطاولة `T15` تبقى الشاشة على `جاري الإرسال...` ولا يكتمل الإرسال.
- الظاهر في الواجهة: `دفعة طلب: 7d8489d2…` مع بقاء السلة في حالة `قيد الإرسال`.

## Scope
- الواجهة: إرسال الطلب من شاشة الجرسون
- الباك إند: endpoint إرسال الطلب وإنشاء دفعة الطلب

## Hypotheses
1. الطلب يصل إلى الباك إند لكنه يتعطل أثناء الحفظ أو إنشاء الدفعة.
2. الطلب لا يخرج من الواجهة أو لا تُغلق حالة الانتظار بعد الاستجابة.
3. يوجد انتظار على ملف أو SQL يجعل endpoint لا يرجع.
4. الاستجابة تحتوي خطأ لا يُعرض في الواجهة فتظل الحالة معلقة.
5. بيانات المقاعد الحالية في الحمولة تدفع المسار إلى حالة غير مغطاة.

## Evidence Plan
- إضافة instrumentation في الواجهة قبل/بعد استدعاء الإرسال.
- إضافة instrumentation في الباك إند عند دخول endpoint وقبل/بعد نقاط الحفظ الحرجة.
- إعادة الإنتاج على `T15` ثم مقارنة ما قبل/بعد نقطة التعليق.

## Status
- Started

## Evidence Collected
- إعادة تنفيذ `POST /api/restaurant/table-sessions` على `T15` أظهرت أولاً timeout عند `15s` ثم نجحت بعد إعادة تشغيل الخادم.
- سجل التتبع أكد أن مسار الجلسة كان يعيد استخدام الجلسة نفسها:
  - `sessionId = 7d8489d2-c0c1-484b-afb4-807cbf5e377b`
- سجل التتبع أكد أن حفظ `table_sessions` في SQL دخل وخرج بنجاح (`_restaurant_sql_set committed`).
- إعادة تنفيذ `POST /api/restaurant/invoices` مباشرة لنفس الجلسة نجح خلال `1883ms` وأعاد:
  - `kitchenOnly = true`
  - `orderId = 4f96213b-0f33-4d1a-820f-f385a733ca13`

## Current Conclusion
- محرك الإرسال للمطبخ يعمل عند استدعائه مباشرة من الـ API.
- السبب العملي الذي يترك شاشة الجرسون على `قيد الإرسال` هو أن الواجهة لم تكن تضع مهلة قطع على `submitSale()`، وبالتالي عند أي تعليق اتصال/Proxy/Backend تبقى `loading=true` مدة مفتوحة.
- تم تطبيق إصلاح واجهة أدنى:
  - `timeoutMs` على `safeFetch` لمسار `POST /api/restaurant/invoices`
  - تحسين رسالة الخطأ إلى `فشل الإرسال` مع تلميح شبكة واضح
  - إضافة timeout أيضًا لمسار ربط الجلسة بالطاولة
