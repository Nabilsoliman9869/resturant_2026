# Debug Session: t12-cancel-seat
- **Status**: [OPEN]
- **Issue**: على الطاولة T12 زر إلغاء التسكين لا يترك أثرًا ظاهرًا للمستخدم.
- **Expected**: عند الإلغاء المسموح تُزال آثار فوات أوان التسكين، يُفك ارتباط الكابتن، وتعود الطاولة إلى جاهزة. وإذا وُجد مانع تشغيلي يجب أن يظهر رفض واضح.
- **Actual**: المستخدم يضغط `إلغاء التسكين` على T12 ولا يرى أثرًا واضحًا.
- **Scope**: `WaiterTablesPage.tsx` و `backend/api_server.py` ومسار `no-order-watch` للجلسات النشطة على الطاولة T12.
- **Hypotheses**:
  1. الواجهة لا ترسل طلب `reset_ready` أصلًا من شريحة T12.
  2. الطلب يخرج من الواجهة لكن الـ API يرفضه أو يعيده بخطأ لا يظهر للمستخدم.
  3. الطلب ينجح لكن يوجد state لاحق يعيد T12 إلى `occupied` بسبب جلسة أو طلبات مفتوحة على نفس الطاولة.
  4. sessionId المرسل من الشريحة لا يطابق الجلسة النشطة الفعلية على T12.
  5. الرسالة أو التحديث المحلي في الواجهة لا يُحدَّث، فيبدو للمستخدم أن الزر بلا أثر رغم أن الخادم تحرك.
- **Evidence Plan**:
  - فحص instrumentation الحالية في الواجهة والباك إند.
  - تشغيل/التحقق من Debug Server.
  - إعادة إنتاج الضغطة على T12 وجمع logs حيّة.
  - مقارنة الاستقبال في الواجهة مع ما يصل إلى مسار `no-order-watch` وما يحدث بعده.
- **Evidence Collected**:
  - الواجهة أرسلت طلب `reset_ready` فعلًا للجلسة `49200f79-befe-437d-be04-04b54144d3b4`.
  - الباك إند استقبل الطلب وربطه بالطاولة `T12` (`tableId=7B14A454-9070-4513-8B2F-CE5AED71BCE6`) والجلسة بحالة `active`.
  - الاستجابة كانت `HTTP 409` مع رسالة صريحة: `لا يمكن إلغاء التسكين لأن على الطاولة طلبات مفتوحة فعلية. أغلق الطلبات أو عالجها أولاً ثم أعد المحاولة.`
  - تم فحص الطلبات الحية، وتأكد وجود طلب مفتوح فعلي على `T12`:
    - `orderId=308ec3d6-14c5-46ea-b022-455bab9b60a8`
    - `sessionId=5189038b-6ee9-4fd1-8233-06327ded0af5`
    - `status=pending`
- **Hypothesis Status**:
  1. الواجهة لا ترسل `reset_ready`: **مرفوضة**
  2. الطلب يصل لكن الـ API يرفضه: **مؤكدة**
  3. الطلب ينجح ثم تعود الطاولة `occupied`: **مرفوضة لهذه المحاولة**
  4. `sessionId` غير مطابق: **مرفوضة**
  5. الخادم يتحرك لكن الرسالة لا تبدو واضحة للمستخدم: **مرجحة كتفسير UX ثانوي**
