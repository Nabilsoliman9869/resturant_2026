-- ============================================================
-- جدول مستخدمي واجهة «مطاعم» — تم دمج السكربت الكامل في:
--   sql/mat3am_app_users_table_and_seed.sql
-- (إنشاء الجدول + إدراج المستخدمين التجريبيين إن لم يوجدوا)
--
-- أو عبر API: POST /api/dev/bootstrap — مع commit منفصل لجدول المستخدمين في api_server.py
-- ============================================================
