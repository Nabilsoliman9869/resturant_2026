تحديث حي للمشروع - 2026-05-22

1) تنفيذ فعلي لمسار Owner/VIP:
- تم التحقق من ظهور حقول سياسة التسعير في صفحة `/app/developer/settings/restaurant-ops`.
- القالب الأول الخاص بـ Owner/VIP مضبوط حاليًا على:
  - العميل: احمد
  - سياسة التسعير: cost_plus
  - نسبة الزيادة: 10%
  - بدون خدمة: نعم
  - بدون ضريبة: نعم

2) نتيجة الاختبار الخلفي:
- تم تطبيق قالب Owner/VIP على جلسة فعلية محلية.
- تم إنشاء طلب ثم طلب حساب.
- بعد إعادة تشغيل `backend/api_server.py` من النسخة الحالية على المنفذ `2288` نجح التسعير الفعلي كما يجب:
  - صنف تجريبي: بصل أبيض
  - AgentPrice = 18.0
  - الناتج النهائي في الفاتورة = 19.8
  - Service = 0
  - VAT = 0

3) ملاحظة تشغيلية مهمة:
- كان هناك تعارض بين الكود الحالي والسلوك الجاري لأن المنفذ `2288` كان مشغولًا بنسخة قديمة من `api_server.py`.
- تم قتل مستمع `2288` ثم تشغيل الخادم من المسار الحالي:
  - `e:\XTRA_WEB\مطاعم\backend\api_server.py`
- بعد ذلك تطابق السلوك مع الكود الحالي.

4) خلاصة الحالة:
- منطق `billingProfile` يصل إلى الجلسة ويؤثر على `request-bill`.
- الخلل السابق لم يكن من المعادلة نفسها، بل من تشغيل نسخة API قديمة محليًا.
- المسار المحلي الآن مثبت وظيفيًا على النسخة الحالية.

5) تحقق بصري وخدمات محلية:
- تم التحقق الآن بصريًا من الصفحة المحلية:
  - `http://127.0.0.1:9999/app/developer/settings/restaurant-ops`
- القالب الأول يظهر فعليًا بالقيم التالية داخل الواجهة:
  - العميل: احمد
  - سياسة التسعير: `cost_plus`
  - نسبة الزيادة: 10%
  - بدون خدمة: نعم
  - بدون ضريبة: نعم
- تم أيضًا التحقق أن الواجهة المحلية على `9999` تعمل، وأن الباك إند المحلي يرد على:
  - `http://127.0.0.1:2288/api/ping`
  - والناتج: `{"server":"api_server","ok":true,"port":2288}`

6) تنفيذ جديد — عمليات الطاولة وإعادة ترتيب الطلبات:
- تم تبسيط بطاقة `عمليات الطاولة` في شاشة `جرسون الطلبات` بحذف الشروحات الطويلة، والاكتفاء بالأقسام المختصرة:
  - `تحويل`
  - `دمج`
- تم إضافة كارت جديد بعنوان `إعادة ترتيب الطلبات` داخل نفس التبويبة.
- الكارت يعرض بنود الطاولة الحالية من مصدرين:
  - السلة `قيد الإرسال`
  - الطلبات المفتوحة/المرسلة على الجلسة
- لكل بند أصبح متاحًا:
  - رؤية حالته الحالية
  - رؤية المقعد الحالي
  - اختيار مقعد جديد
  - تنفيذ `نقل`
- تم توسيع endpoint الخلفي:
  - `PATCH /api/restaurant/orders/{order_id}/items/{line_id}`
  ليقبل تعديل `seatNo` قبل `طلب الحساب` فقط.
- بعد `طلب الحساب` تبقى إعادة الترتيب مقفلة لمنع تغيير توزيع البنود بعد اعتماد الحساب.

7) تحقق التنفيذ:
- `npm run build` نجح بعد التعديلات.
- `api_server.py` مرّ عبر `py_compile` بنجاح.
- تم تشغيل المحليين بعد التعديل:
  - `2288` للباك إند
  - `9999` للفرونت

8) تنفيذ جديد — طباعة الشيك بصلاحيات مضبوطة:
- تم إضافة تتبع طباعة داخل الفاتورة المحلية نفسها، ويُسجل الآن:
  - `printCount`
  - `firstPrintedAt`
  - `firstPrintedByRole`
  - `lastPrintedAt`
  - `lastPrintedByRole`
- تمت إضافة endpoint جديد:
  - `POST /api/restaurant/invoices-local/mark-printed`
- قاعدة التشغيل الحالية أصبحت:
  - `waiter / الكابتن`: طباعة واحدة فقط لكل شيك
  - `cashier / manager / developer`: مسموح لهم بإعادة الطباعة
- بعد استهلاك طباعة الكابتن الأولى، يمنع النظام أي طباعة لاحقة له على نفس الفاتورة، مع رسالة واضحة بأن إعادة الطباعة تكون من الكاشير أو المدير.
- شاشة `جرسون الطلبات` تعرض الآن داخل `عمليات الطاولة` قسمًا جديدًا:
  - `شيكات الطاولة`
  - ويظهر بعد `طلب الحساب`
  - مع معاينة/طباعة لكل شيك صادر على الجلسة
- مودال الفاتورة أصبح يعمل بوضعين:
  - وضع الكابتن: معاينة + طباعة فقط بدون تسديد
  - وضع الكاشير: معاينة + طباعة + تسديد
- شاشة `فواتير المطعم (كاشير)` أصبحت تفتح الفاتورة أيضًا للفواتير المسددة أو المنتظرة، بحيث تبقى إعادة الطباعة متاحة للكاشير من نفس الشاشة.

9) تنفيذ جديد — مرحلة تأسيس نظام الشيف المختص:
- تمت إضافة خيار جديد داخل صفحة:
  - `/app/developer/settings/restaurant-ops`
  لاختيار نمط تشغيل المطبخ بين:
  - `استخدام النظام الحالي`
  - `استخدام نظام الشيف المختص`
- عند اختيار `نظام الشيف المختص` تظهر الآن بطاقة إعدادات جديدة داخل نفس الصفحة لتعريف:
  - اسم الشيف / اسم المحطة
  - توصيف العمل (مثال: شاورما وبرجر)
  - حالة التفعيل
  - الأصناف التي يختص بها الشيف من قائمة الأصناف
- تم ربط هذه الإعدادات بمفاتيح جديدة داخل `ops-settings`:
  - `kitchenExecutionMode`
  - `kitchenSpecialistChefsJson`
- الحفظ يتم من نفس مسار الإعدادات الحالي:
  - `GET/PUT /api/restaurant/ops-settings`
  دون فتح مسار منفصل جديد.
- شاشة المطبخ الحالية بقيت كما هي لمدير المطبخ وتقرأ هذا الوضع الجديد قراءة أولية فقط:
  - إذا كان `نظام الشيف المختص` مفعّلًا، يظهر تنبيه واضح أن هذه الشاشة ما زالت شاشة المتابعة العامة، مع عدد الشيفات/المحطات المختصة المفعلة.
- تم فحص هذه المرحلة بنجاح:
  - `GetDiagnostics` نظيف
  - `npm run build` نجح
  - `api_server.py` مر عبر `py_compile` بنجاح

10) تنفيذ جديد — دور مستقل للشيف المختص وربطه بالمستخدمين:
- تمت إضافة دور جديد رسمي في النظام:
  - `kitchen_specialist`
  - التسمية الظاهرة: `شيف مختص`
- هذا الدور أصبح مدعومًا الآن في:
  - تسجيل الدخول
  - توجيه المسارات
  - شاشة المستخدمين
  - جدولة الأدوار
  - bootstrap الافتراضي للمستخدمين
- تمت إضافة مسار مستقل للشيف المختص:
  - `/app/kitchen_specialist/kitchen`
- إعدادات `نظام الشيف المختص` لم تعد تعتمد على اسم حر فقط، بل صار كل صف شيف يدعم ربطًا مباشرًا مع مستخدم فعلي من:
  - `MAT3AM_APP_USERS`
  - بشرط أن يكون دوره `kitchen_specialist`
- سجل الشيف المختص في `ops-settings` صار يدعم الآن:
  - `userId`
  - `userLogin`
  - إضافة إلى الاسم الوصفي والأصناف التابعة له
- شاشة الشيف المختص تقرأ الآن المستخدم الحالي من الجلسة، وتبحث عن صفه التشغيلي داخل إعدادات `kitchenSpecialistChefsJson`، ثم تعرض فقط البنود التابعة للأصناف المسندة له.
- شاشة مدير المطبخ العامة بقيت كما هي ولم تُكسر، وتبقى شاشة المتابعة الكلية، بينما الشيف المختص يرى فقط حصته التشغيلية.
- تم فحص هذه المرحلة بنجاح:
  - `GetDiagnostics` نظيف
  - `npm run build` نجح
  - `api_server.py` و`init_concreet_db.py` مرا عبر `py_compile` بنجاح

11) تنفيذ جديد — تحويل الشيف المختص إلى إسناد بالمحطة التشغيلية:
- تم استكمال التحول البنيوي من ربط الشيف المختص بالمستخدم الفردي إلى ربطه بمحطة تشغيلية مشتركة.
- صفحة `/app/developer/settings/restaurant-ops` أصبحت تعتمد الآن على:
  - `stationCode`
  - بدل اختيار مستخدم فردي داخل صف الشيف المختص
- كل صف في `نظام الشيف المختص` أصبح يمثل:
  - محطة تشغيلية / دورًا تشغيليًا مشتركًا
  - مع اسم وصفي
  - وتوصيف عمل
  - وحالة تفعيل
  - والأصناف المسندة لهذه المحطة
- عند حفظ الإعدادات، يتم الآن تنظيف بيانات الربط الفردي القديمة (`userId` / `userLogin`) داخل صفوف الشيف المختص، والإبقاء على الإسناد التشغيلي عبر `stationCode` فقط.
- صفحة الإعدادات تعرض الآن المستخدمين الفعليين المرتبطين بكل محطة تشغيلية بحسب قيمة:
  - `SpecialistStationCode`
  - داخل `MAT3AM_APP_USERS`
- شاشة `/app/kitchen_specialist/kitchen` أصبحت تقرأ:
  - `specialistStationCode`
  - من جلسة المستخدم الحالي
  - ثم تطابقه مع `stationCode`
  - داخل `kitchenSpecialistChefsJson`
- نتيجة ذلك:
  - إذا كان على نفس المحطة أكثر من مستخدم بدور `kitchen_specialist`
  - فإنهم يرون نفس الأصناف المسندة لنفس المحطة
  - دون تكرار أو تضارب في إسناد الصنف الواحد
- أُبقي fallback احتياطي في شاشة الشيف المختص لقراءة الربط الفردي القديم فقط لحماية البيانات القديمة مؤقتًا أثناء الانتقال، لكن المصدر التشغيلي المعتمد أصبح هو `stationCode`.
- التحقق النهائي لهذه المرحلة:
  - `GetDiagnostics` نظيف في:
    - `RestaurantOpsSettingsPage.tsx`
    - `KitchenPage.tsx`
    - `api_server.py`
    - `mat3am_sql_cache.py`
  - `npm run build` نجح
  - تعذر تنفيذ `py_compile` من الطرفية الحالية لعدم توفر أمر Python في البيئة نفسها، لكن تشخيص ملفات الباك إند من المحرر كان نظيفًا.

12) تنفيذ جديد — عدّاد جزئي للشيف المختص (خلص 1):
- تم توسيع نموذج بند المطبخ ليدعم `preparedQty` داخل السطر بدل الاعتماد فقط على `prepared` كقيمة نعم/لا.
- هذا يتيح تنفيذ جزئي صحيح من نفس السطر عندما تكون الكمية أكبر من 1، مثل:
  - `1 من 6`
  - `3 من 12`
- تمت إضافة منطق تطبيع وتحديث في `backend/api_server.py` بحيث:
  - `preparedQty` يُقص إلى حدود الكمية الفعلية
  - إذا وصل إلى كامل الكمية يصبح السطر `ready`
  - وإذا كان أكبر من 0 وأقل من الكمية يبقى السطر في حالة تقدم (`preparing`)
- شاشة `KitchenPage.tsx` أصبحت تعرض في الملخص التراكمي لكل صنف:
  - المتبقي
  - المنجز من الإجمالي
  - وزر سريع `خلص 1`
- زر `خلص 1` يطبق زيادة واحدة على أقدم بند مطابق داخل نفس الصنف، بدل الدخول لكل كارت وسطر يدويًا.
- تم أيضًا إظهار تقدم السطر نفسه عندما تكون كميته أكبر من 1 بصيغة:
  - `التقدّم: 3/6`
- تم فحص هذه المرحلة بنجاح:
  - `GetDiagnostics` نظيف في `KitchenPage.tsx` و `api_server.py`
  - `npm run build` نجح
  - تم إعادة تشغيل الخادم من `backend/api_server.py` بعد التعديل

13) تنفيذ جديد — توحيد التشغيل بين EXE ونسخة 9999 + مرجع مركزي للمحطات:
- تم توحيد مسار البيانات القابل للكتابة بين EXE والتشغيل المحلي:
  - `api_server.py` أصبح يختار `DATA_DIR` الدائم تلقائياً إذا وُجد مخزن سابق في `%LOCALAPPDATA%\Mat3amPOS`
  - `run_api.bat` و `run_full_stack.bat` يضبطان `MAT3AM_BASE_DIR=%LOCALAPPDATA%\Mat3amPOS` افتراضياً
- النتيجة:
  - نسخة EXE ونسخة 9999 تستخدمان نفس `config/settings.json`
  - ونفس ملفات `config/restaurant/*`
  - فلا يبقى اختلاف السلوك بسبب اختلاف DATA_DIR فقط
- تم إدخال مرجع مركزي جديد لمحطات الشيف المختص:
  - `kitchenSpecialistStationsJson`
  - ويصبح هو المصدر الوحيد لتعريف المحطات
- `kitchenSpecialistChefsJson` لم يعد مكان إنشاء المحطة، بل مكان ربط الأصناف بمحطة معرفة مسبقاً
- تم تعديل `RestaurantOpsSettingsPage.tsx` بحيث:
  - تُعرّف المحطات أولاً في قسم مستقل
  - ثم تُربط الأصناف بكل محطة من نفس المرجع
- تم تعديل `DeveloperUsers.tsx` بحيث:
  - مستخدم `kitchen_specialist` يختار محطته من قائمة المحطات المركزية
  - ولم يعد الحقل نصاً حراً
- تم تعديل `backend/api_server.py` بحيث:
  - يرفض إنشاء/تحديث `specialistStationCode` إذا لم تكن المحطة معرّفة في المرجع المركزي
  - ويشتق المحطات تلقائياً من البيانات القديمة عند الحاجة كمسار انتقال
- تم تعديل `KitchenPage.tsx` لقراءة اسم المحطة من المرجع المركزي وعرضه للمستخدم المختص
- فحص هذه المرحلة:
  - `GetDiagnostics` نظيف في:
    - `RestaurantOpsSettingsPage.tsx`
    - `DeveloperUsers.tsx`
    - `KitchenPage.tsx`
    - `api_server.py`
  - `npm run build` نجح

14) تنفيذ جديد — تصحيح عدّاد المطبخ ليعتمد على أكبر زمن تحضير:
- تم تعديل منطق أوامر KDS في `backend/api_server.py` بحيث `prepTargetMinutes` لا يُفهم كمجموع أزمنة الأصناف.
- الحساب الجديد يعتمد على:
  - استخراج زمن التحضير لكل صنف من `TBL007.Hieght3` / `PrepMinutes`
  - ثم اعتماد **أكبر زمن تحضير** بين أصناف التذكرة كزمن مستهدف للطاولة
- تم تطبيق ذلك في:
  - إنشاء تذكرة مطبخ جديدة
  - دمج بنود جديدة على تذكرة مفتوحة
  - إعادة قراءة/تطبيع الطلبات المفتوحة القديمة عبر `_kds_refresh_order_status`
- الأثر التشغيلي:
  - إذا احتوت الطاولة على 4 أصناف أزمنتها 10 و12 و20 و25 دقيقة
  - فالعدّاد المستهدف يصبح 25 دقيقة
  - وليس 67 أو 80 دقيقة حسب الجمع
- التحقق:
  - `GetDiagnostics` نظيف في `backend/api_server.py`
  - `py_compile api_server.py` نجح
  - تم تشغيل الخادم محلياً على `2288`

15) تنفيذ جديد — نموذج مدير المطبخ: ETA تنبؤي + أولوية ذكية:
- تم تعديل `src/pages/KitchenPage.tsx` لمدير المطبخ بحيث:
  - يتم فصل `زمن خدمة العميل المتوقع` عن `بدء التنفيذ الفعلي`
  - `بدء التحضير` في الوضع العام صار يُعرض للمستخدم كـ `بدء التنفيذ`
  - بينما ETA للطاولة يُحسب تنبؤياً من وقت دخول الطلب وزمنه المستهدف
- تمت إضافة طبقة تقييم لكل طلب:
  - `clientRemainingSec`
  - `clientOverdue`
  - `clientAtRisk`
  - `pendingQty`
  - `priorityScore`
- وتمت إضافة طبقة تقييم لكل طاولة:
  - نسبة الإكمال
  - الخطر (`متأخرة` / `معرّضة` / `قيد التنفيذ` / `آمنة`)
  - ETA للطاولة
  - حجم البنود المفتوحة
- شاشة المطبخ العامة أصبحت تعرض في الأعلى:
  - الطاولات المفتوحة
  - المتأخرة
  - المعرضة
  - قيد التنفيذ
  - البنود المفتوحة
  - ضغط التشغيل
- الكروت نفسها أصبحت ترتب حسب أولوية ذكية بدل الترتيب الخام:
  - خطر التأخير أولاً
  - ثم الزمن المتبقي
  - ثم عمر الطلب
- الشريط الجانبي `طلبات حسب الطاولة` صار يرتب أيضاً حسب الأولوية ويعرض:
  - حالة الخطر
  - ETA للطاولة
  - عدد البنود المفتوحة
- نظام الشيف المختص بقي على منطق التنفيذ الكمي الجزئي، ولم يتم خلطه مع منطق مدير المطبخ
- التحقق:
  - `GetDiagnostics` نظيف في `KitchenPage.tsx`
  - `npm run build` نجح

16) تنفيذ جديد — تصنيف مرتجع الضيوف وشاشة مراجعة المدير:
- تم توسيع `src/lib/guestReturnCatalog.ts` ليعرّف:
  - مرحلة البند `pending / preparing / ready / served / ...`
  - نوع المرتجع:
    - `cancel_before_prep`
    - `quality_after_service`
    - `sealed_unused_return`
  - نمط الاعتماد:
    - `cancel_if_not_started`
    - `manager_review_required`
    - `direct_accept_recommended`
- تم تعديل `src/pages/WaiterOrderPage.tsx` لتمرير `lineStatus` و`orderStatus` لكل بند عند فتح مرتجع الضيف.
- تم تعديل `src/components/GuestReturnRequestModal.tsx` بحيث:
  - يظهر للجرسون سياق كل بند قبل الإرسال
  - يعرض مرحلة البند
  - يعرض نوع المرتجع
  - يعرض نمط الاعتماد المتوقع
  - يعرض التوجيه المقترح للمعالجة
  - ويُرسل هذه البيانات إلى الخادم مع الطلب
- تم تعديل `backend/api_server.py` بحيث يحفظ مع كل بند مرتجع:
  - `lineStatus`
  - `orderStatus`
  - `itemStage`
  - `returnKind`
  - `approvalMode`
  - `recommendedDisposition`
  - `policyHint`
  - وملخصًا على مستوى الطلب `reviewSummary`
- تم تعديل `src/pages/GuestReturnsManagerPage.tsx` لتتحول من اعتماد أعمى إلى شاشة مراجعة قرار:
  - تعرض نوع الحالة لكل بند
  - تعرض مرحلة البند
  - تعرض نمط الاعتماد
  - تعرض ملاحظة الكابتن
  - تسمح للمدير باختيار القرار النهائي لكل بند
  - وتسمح بإضافة ملاحظة مدير قبل الاعتماد أو الرفض
- تم تثبيت اعتماد القرار النهائي من الشاشة إلى `PATCH /api/restaurant/guest-returns/{id}` مع تمرير `finalDisposition` لكل بند.
- ملاحظة مهمة:
  - هذه المرحلة حسّنت التصنيف والمراجعة والشفافية
  - لكنها لم تربط المرتجع بعد بالأثر المالي المباشر على فاتورة الطاولة؛ هذه تبقى المرحلة التالية
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `GuestReturnRequestModal.tsx`
    - `GuestReturnsManagerPage.tsx`
    - `guestReturnCatalog.ts`
    - `api_server.py`
  - `npm run build` نجح

17) تنفيذ جديد — ربط مرتجع الضيوف بالأثر المالي على الفاتورة:
- تم ربط `backend/api_server.py` بحيث تؤثر بنود `Guest Return` المعتمدة ماليًا على مسار الفاتورة بطريقتين:
  - قبل `request-bill`: يتم خصم الكمية المعتمدة من البند نفسه قبل بناء الفاتورة، فلا يدخل الجزء المرتجع أصلًا ضمن الاستحقاق.
  - بعد `request-bill`: الفاتورة المحلية تقرأ الآن `sourceLines` التفصيلية، ثم تضيف تسوية سالبة واضحة بعنوان:
    - `تسوية مرتجع: اسم الصنف`
    مع تخفيض مباشر في إجمالي الفاتورة المحلية وقيمة التحصيل.
- تم إضافة دوال مساعدة جديدة لتحليل البنود المعتمدة من:
  - `guest_return_requests`
  - واستخراج المعالجات التي ترفع العبء عن الضيف مثل:
    - `stock_return`
    - `deduct_waiter`

18) تنفيذ جديد — إصلاح تعليق/فشل حفظ ops-settings:
- تم تتبع عطل شاشة:
  - `محطات الشيف المختص وتشغيل المطبخ`
  - عند الضغط على `حفظ الكل`
- النتيجة الفعلية بعد التشخيص:
  - `GET /api/restaurant/ops-settings` كان يعمل
  - لكن `PUT /api/restaurant/ops-settings` كان يعلق داخل كتابة ملف JSON المحلي بعد نجاح SQL
  - السبب لم يعد فقط `PermissionError` الظاهر سابقًا، بل إن كتابة النسخة المحلية إلى ملفات `%LOCALAPPDATA%\Mat3amPOS\config\restaurant\*.json` قد تتوقف/تُحتجز على ويندوز
- تم تعديل `backend/api_server.py` في مساري:
  - `_workflow_sql_write`
  - `_ops_sql_write`
  بحيث:
  - تم استبدال `MERGE` بمسار أوضح: `UPDATE ثم INSERT عند عدم وجود السطر`
  - تمت إضافة `SET LOCK_TIMEOUT 5000`
  - وتمت إضافة تسجيل واضح لسبب الفشل في SQL بدل الإرجاع الصامت
- وتم تعديل:
  - `_restaurant_write_workflow`
  - `_restaurant_write_ops`
  بحيث يصبح الحفظ:
  - `SQL-first` فعليًا
  - إذا نجح SQL يعاد الرد مباشرة دون انتظار كتابة JSON المحلي
  - وتبقى كتابة JSON مسار fallback فقط عندما لا ينجح SQL
- التحقق التشغيلي:
  - إعادة تشغيل `backend/api_server.py` من المسار الحالي نجحت
  - `__whoami__` أكد أن النسخة العاملة هي:
    - `E:\XTRA_WEB\مطاعم\backend\api_server.py`
  - اختبار `PUT /api/restaurant/ops-settings` بمفتاح واحد نجح
  - اختبار إعادة حفظ الحمولة الكاملة نفسها نجح أيضًا
- الخلاصة:
  - سبب `HTTP 500`/تعليق `حفظ الكل` عولج من المصدر
  - المسار المعتمد الآن هو SQL كمرجع أول، مع إبقاء JSON كبديل احتياطي فقط عند الحاجة

19) تنفيذ جديد — إصدار ملف EXE رقم 25:
- تم تنفيذ مسار البناء الكامل من مجلد المشروع الحالي باستخدام:
  - `prepare_mat3am_exe_build.py`
  - `npm run build`
  - `PyInstaller`
  - `version_exe_artifact.py`
- تم تحديث طابع البناء في:
  - `config/mat3am_exe_build.txt`
  - والقيمة الحالية:
    - `2026-05-24T06:45:13Z code=M260524-0645-D98D7DF git=d98d7df`
- تم تحديث ملفات موارد التنفيذ أيضًا:
  - `assets/mat3am_icon.ico`
  - `assets/file_version_info.txt`
- خرجت النسخة العامة:
  - `dist/Mat3amPOS.exe`
- وتم إنشاء النسخة المرقمة المطلوبة بنجاح:
  - `dist/Mat3amPOS025.exe`
- بيانات الملف الناتج:
  - الحجم: `24,764,079` بايت
  - وقت الإنشاء المحلي: `2026-05-24 09:47`
- ملاحظة البناء:
  - ظهر تحذير Vite فقط بخصوص حجم chunk أكبر من 500KB، لكنه لم يمنع البناء ولا أوقف إصدار EXE

20) تنفيذ جديد — فصل عرض وطباعة بنود الحساب حسب الكرسي:
- تم تعديل `src/components/CaptainBillReviewModal.tsx` لمعالجة مشكلتين مباشرتين:
  - تثبيت ألوان النصوص والعناوين داخل مودال مراجعة طلب الحساب بحيث تبقى واضحة على الخلفية الداكنة
  - تحويل عرض البنود من جدول واحد عام إلى أقسام مستقلة حسب الكرسي (`كرسي 1`، `كرسي 2`...، أو `بدون كرسي`)
- كل قسم في مراجعة الكابتن أصبح يعرض:
  - بنود هذا الكرسي فقط
  - عدد البنود
  - إجمالي الكرسي نفسه
- تم تعديل `src/components/CashierPayInvoiceModal.tsx` ليقرأ `sourceLines` من الفاتورة المحلية بدل الاكتفاء بـ `lines` المجمعة، لأن `sourceLines` تحتفظ بـ `seatNo`
- تم توسيع نموذج سطر الفاتورة محليًا ليدعم:
  - `seatNo`
  - وقراءة `sourceLines`
- تم تعديل تجميع البنود بحيث لا يدمج صنف الكرسي 1 مع نفس الصنف على الكرسي 2، بل يبقي الفصل حسب `seatNo`
- شاشة الفاتورة/الطباعة أصبحت تعرض البنود داخل أقسام حسب الكرسي عندما تكون بيانات الكراسي موجودة
- الإيصال الحراري المبني في `buildThermalReceiptHtml` صار يدعم أيضًا أقسامًا مستقلة لكل كرسي بدل جدول واحد مجمّع، ما دام `seatNo` موجودًا في البيانات
- تم دعم المسار الاحتياطي أيضًا:
  - عند استخراج البنود من طلبات الجلسة مباشرة، يتم تمرير `seatNo` إلى التجميع بدل فقدانه
- التحقق:
  - `GetDiagnostics` نظيف في الملفات المعدلة
  - `npm run build` نجح بعد التعديل
    - `deduct_kitchen`
    - `shift_charge`
- تم تحديث:
  - `GET /api/restaurant/invoices-local`
  - `GET /api/restaurant/invoices-local/by-id/{invoice_id}`
  - `POST /api/restaurant/invoices-local/mark-paid`
  لكي تعكس التسويات المعتمدة قبل العرض وقبل تسجيل مبلغ السداد النهائي.
- تم حفظ `sourceLines` داخل السجل المحلي لكل فاتورة ناشئة من `request-bill` حتى تبقى مطابقة المرتجع دقيقة حتى مع:
  - تقسيم الشيكات حسب المقاعد
  - وتوزيع الطلب المشترك
- تم إغلاق ثغرات محاسبية مرتبطة بطلب الحساب:
  - منع `request-bill` إذا كانت السلة تحتوي بنودًا غير مرسلة
  - منع تعديل الجلسة/المقاعد/النقل بعد `billingRequestedAt`
  - منع دمج الجلسات بعد طلب الحساب
- تم تعديل `WaiterOrderPage.tsx` لرفض `طلب الحساب` محليًا إذا بقيت بنود داخل السلة غير مرسلة، مع تمرير `pendingCartCount` للباك إند كطبقة تحقق إضافية.
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `api_server.py`
    - `WaiterOrderPage.tsx`
  - `npm run build` نجح

21) تشخيص جديد — سبب غرابة «ابدأ التسكين» وبطئه:
- تم قياس الأزمنة الفعلية للمسارات المرتبطة بشاشة الطاولات محليًا:
  - `GET /api/restaurant/tables` حوالي `1915ms`
  - `GET /api/restaurant/operational-snapshot` حوالي `637ms`
  - `GET /api/restaurant/table-sessions?status=active` حوالي `4037ms`
- النتيجة:
  - الإحساس ببطء `ابدأ التسكين` كان حقيقيًا
  - أبطأ مسار حاليًا هو تحميل `الجلسات النشطة`
  - وليس `operational-snapshot`
- تم تحديد خلل واجهة مباشر في `src/pages/WaiterTablesPage.tsx`:
  - حالة الانشغال كانت محفوظة في متغير عام واحد `claimBusy`
  - لذلك عند الضغط على `ابدأ التسكين` لطاولة واحدة كانت كل بطاقات الطاولات تدخل حالة busy/disabled بصريًا
  - وهذا أعطى انطباعًا أن الأمر يُرسل لكل الطاولات
- تم تنفيذ إصلاح محصور:
  - استبدال `claimBusy` بـ `claimBusyTableId`
  - بحيث تصبح حالة الانشغال مرتبطة بالطاولة المضغوط عليها فقط
  - مع إبقاء بقية منطق البطاقة كما هو
- التحقق بعد الإصلاح:
  - `GetDiagnostics` نظيف في `WaiterTablesPage.tsx`
  - `npm run build` نجح
- المراجعة التاريخية عبر Git أظهرت أن التحول الكبير في منطق التسكين دخل في commit:
  - `0ea0846`
  - حيث تغير سلوك الزر من ربط جلسة موجودة فقط إلى إنشاء جلسة جديدة أيضًا عند غيابها (`captain_seat_from_table_card`)

22) تنفيذ جديد — تثبيت منطق إلغاء التسكين وحل تعليق الحفظ:
- تم فتح جلسة debug مستقلة `debug-cancel-seating-stuck.md` لأن زر `إلغاء التسكين` كان يرسل الطلب فعليًا لكن التنفيذ يعلق قبل اكتمال إغلاق الجلسة.
- الدليل runtime أثبت أن:
  - الطلب يصل إلى `POST /api/restaurant/table-sessions/{session_id}/no-order-watch`
  - ثم يدخل `_restaurant_complete_session_internal`
  - لكنه كان يعلق في مسارات الحفظ/المزامنة الخلفية بدل إنهاء الجلسة سريعًا
- تم تعديل `backend/api_server.py` في التخزين المشترك:
  - إضافة `LOCK_TIMEOUT` و`cursor.timeout` في `_restaurant_sql_set`
  - اعتماد مسار fast-local لـ `table_sessions` و`tables`:
    - يثبت override محليًا داخل الخادم فورًا
    - ثم يرسل مزامنة JSON وSQL في الخلفية بدل حبس طلب الإلغاء
- النتيجة التشغيلية بعد التعديل:
  - جلسة `T12` الحالية خرجت من `active`
  - ومسار `complete_session saved` صار يظهر في سجل debug بدل التوقف قبل الحفظ
  - كما اختفى `noOrderOverdue` من بيانات الطاولة
- تم اكتشاف سبب بقاء `T12` بحالة `occupied` بعد ذلك:
  - توجد طلبات `pending` فعلية على نفس الطاولة لكنها مرتبطة بجلسة أقدم `5189038b-6ee9-4fd1-8233-06327ded0af5`
  - لذلك لم يكن من الصحيح اعتبار الطاولة `جاهزة` رغم غلق الجلسة الحالية
- تم تثبيت قاعدة الباك إند وفق مطلب التشغيل:
  - `reset_ready` يرفض الآن أيضًا إذا وُجدت على الطاولة نفسها طلبات مفتوحة (`pending/preparing/ready`) حتى لو كانت من جلسة أقدم
  - ويرجع `409` برسالة واضحة بدل السماح بإلغاء تسكين مضلل
- الخلاصة:
  - تم حل تعليق الزر نفسه ومسار الإغلاق
  - وتم تصحيح قاعدة القرار: لا إلغاء تسكين لطاولة عليها طلبات مفتوحة فعلية
- الخلاصة التشغيلية:
  - المشكلة كانت مركبة بين بطء حقيقي في إعادة تحميل الجلسات + سلوك busy عام على كل الطاولات
  - تم إصلاح الجزء البصري/التفاعلي الآن
  - وما زال مسار `table-sessions?status=active` هو المرشح الأول للتخفيف إذا أردنا تسريع التجربة أكثر
- تحسين متابعة لاحق على نفس المسار:
  - تم تعديل `claimCaptain` في `WaiterTablesPage.tsx` ليطبق تحديثًا محليًا فوريًا للجلسة بعد نجاح `POST /table-sessions` أو `claim-order-taker`
  - الإجراء أصبح يحدّث:
    - `sessions`
    - `sessionByTable`
    - `busyIds`
    - وحالة الطاولة إلى `occupied`
  - ثم يطلق `loadTables()` في الخلفية دون انتظار حاجز
  - النتيجة المتوقعة:
    - استجابة أسرع بصريًا مباشرة بعد ضغط `ابدأ التسكين`
    - مع بقاء التزامن الكامل لاحقًا عبر إعادة التحميل الخلفية
  - التحقق:
    - `GetDiagnostics` نظيف في `WaiterTablesPage.tsx`
    - `npm run build` نجح
  - تخفيف جديد من الباك إند:
    - تم تعديل `GET /api/restaurant/table-sessions` في `backend/api_server.py`
    - المسار كان ما يزال يشغّل `_restaurant_sync_no_order_watch_alerts()` بشكل متزامن داخل الطلب نفسه
    - تم فصله إلى background thread باسم:
      - `mat3am-no-order-watch-sync-sessions`
    - الهدف:
      - تقليل زمن `table-sessions?status=active`
      - ومن ثم تقليل تأخر `ابدأ التسكين` وقراءة شاشة الطاولات
  - التحقق بعد هذا التخفيف:
    - `GetDiagnostics` نظيف في `api_server.py`
    - `npm run build` نجح

18) تنفيذ جديد — إعادة تبويب الإعدادات والإدارات بشكل مرقّم:
- تم إعادة تنظيم `src/pages/settings/SettingsLayout.tsx` ليصبح مرجعًا أوضح ومرقّمًا للتدريب، بدل العناوين القديمة المبعثرة.
- التقسيم الجديد داخل مركز الإعدادات أصبح كالتالي:
  - `1. إعدادات الصالة`
  - `2. إعدادات المطبخ والطباعة`
  - `3. إعدادات المستخدمين والتشغيل`
  - `4. الإعدادات المالية والبيع`
  - `5. الكتالوج والتعريفات`
  - `6. إعدادات النظام`
  - `7. المراكز الإدارية المرتبطة`
- كل عنصر داخل هذه الأقسام صار يحمل ترقيمًا فرعيًا مثل:
  - `1.1`
  - `1.2`
  - `4.7`
  لتسهيل الشرح للمطورين وتدريب المستخدمين.
- تم كذلك ضم الشاشات التي كانت مبعثرة خارج الإعدادات داخل قسم مرجعي واضح بعنوان:
  - `المراكز الإدارية المرتبطة`
  ويشمل روابط مباشرة إلى:
  - `مرتجعات الضيوف`
  - `Call Center (دليفري)`
  - `إدارة الدليفري`
  - `المشتريات`
  - `صرف مصروفات`
  - `تقارير الحسابات`
  - `التدفق النقدي`
- تم إعادة تنظيم `src/components/AppShell.tsx` أيضًا بحيث لم تعد القائمة الرئيسية تظهر كسلسلة روابط متتابعة فقط، بل صارت مجمعة في مجموعات مرقمة بحسب المنطق التشغيلي لكل دور.
- بالنسبة للمدير والمطور، صارت القائمة الرئيسية مقسمة إلى:
  - `1. الصالة والتشغيل الأمامي`
  - `2. خدمة العميل والدليفري`

22) تنفيذ جديد — تثبيت سلوك إلغاء التسكين بلا طلبات:
- تم تثبيت القاعدة التشغيلية التالية:
  - لا يجوز إلغاء التسكين إذا كانت الجلسة تحتوي طلبات فعلية
  - وإذا لم توجد طلبات، فإن إلغاء التسكين يجب أن:
    - يمسح أثر `فوات أوان التسكين`
    - يفك ارتباط الكابتن من الجلسة
    - ويعيد الطاولة إلى `جاهزة`
- الباك إند كان يرفض الإلغاء عند وجود طلبات أصلًا عبر مسار:
  - `POST /api/restaurant/table-sessions/{session_id}/no-order-watch`
  - مع رد `409` إذا وُجدت طلبات على الجلسة
- تم تعديل `backend/api_server.py` بحيث:
  - `_restaurant_complete_session_internal` يمسح حقول `noOrder*` عند إكمال الجلسة
  - ويدعم تفريغ حقول الكابتن (`captainUserId/login/name/role/claimedAt`) عند إلغاء التسكين
  - ومسار `reset_ready` يفعّل هذا التفريغ ويعيد الطاولة إلى `ready`
- تم تعديل `src/pages/WaiterTablesPage.tsx` لتوحيد الصياغة الظاهرة للمستخدم من:
  - `إرجاع الطاولة جاهزة / إنهاء التسكين`
  - إلى معنى أوضح:
    - `إلغاء التسكين`
  - مع رسالة نجاح:
    - `تم إلغاء التسكين وإرجاع الطاولة إلى جاهزة`
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `api_server.py`
    - `WaiterTablesPage.tsx`
  - `py_compile api_server.py` نجح
  - `3. مركز الإعدادات`
  - `4. المالية والتشغيل الخلفي`
- بالنسبة للكاشير والمحاسب وبقية الأدوار، تم اعتماد نفس الفكرة مع عناوين مرقمة مناسبة لطبيعة العمل لكل دور.
- هذا التنفيذ حافظ على المسارات الحالية ولم يكسر الصفحات أو يعِد تسمية الـ routes، بل ركّز على:
  - ترتيب التنقل
  - توحيد المنطق
  - تسهيل التدريب
  - تقليل الإحساس بالتبعثر
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `SettingsLayout.tsx`
    - `AppShell.tsx`
  - `npm run build` نجح

19) تنفيذ جديد — متابعة الطاولة المسكنة بلا طلبات:
- تم إدخال مسار تشغيلي جديد لمعالجة حالة الطاولة المسكنة التي لم يُسجل عليها أي طلب بعد مرور 10 دقائق.
- الباك إند أصبح ينشئ متابعة تشغيلية على مستوى الجلسة نفسها بدل الاكتفاء بعدّاد بصري فقط.
- تم اعتماد ثلاث مراحل تشغيلية:
  - التنبيه الأول للكابتن
  - التصعيد للكابتن والمدير بعد انتهاء الغفوة الأولى
  - الحسم الإجباري بعد انتهاء الغفوة الأخيرة
- أضيفت على الجلسة حقول متابعة مثل:
  - `noOrderSnoozedUntil`
  - `noOrderSnoozeCount`
  - `noOrderWatchStage`
  - تواريخ أول تنبيه / التصعيد / الحسم
- تمت إضافة endpoint جديد:
  - `POST /api/restaurant/table-sessions/{session_id}/no-order-watch`
  ويقبل:
  - `action = snooze`
  - `action = close`
  مع `reason` عند إنهاء التسكين.
- تم توسيع الجرس الأحمر `role_inbox` ليدعم عناصر متابعة التسكين بلا طلبات، مع أزرار مباشرة:
  - `غفوة 10 د`
  - `إنهاء التسكين`
- تم تعديل شريحة الطاولة في `WaiterTablesPage.tsx` لتعرض حالة تشغيلية أوضح بدل العدّاد الصامت فقط، مثل:
  - `بلا طلبات 10 د`
  - `غفوة حتى ...`
  - `مصعّدة للمدير`
  - `حسم مطلوب`
- تم كذلك توحيد احتساب `قيمة تقديرية` وتقرير الطاولة بحيث يفضّل `sessionId` عند وجود جلسة نشطة، لتقليل التناقض بين الجلسة الحالية وبقايا طلبات مرتبطة بالطاولة نفسها من جلسات أخرى.
- تم توسيع إغلاق الجلسة ليدعم حفظ:
  - `completionReason`
  - وبيانات المستخدم الذي أغلق التسكين
  مع تسجيل ذلك في `session_audit`.
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `api_server.py`
    - `WaiterTablesPage.tsx`
    - `RestaurantDualBells.tsx`
  - `npm run build` نجح

20) تنفيذ جديد — تحويل طلب الحساب والحد الأدنى إلى حالة تشغيلية:
- تم توسيع قراءة شريحة الطاولة لتتعامل مع `billingRequestedAt` كعمر تشغيلي بدل شارة ثابتة فقط.
- بطاقة الطاولة تعرض الآن:
  - `طلب الحساب X د`
  - `ناقص حد أدنى X ج`
  - وإذا اجتمعا يظهران كسياق واحد داخل منطقة التنبيه التشغيلي.
- إذا طال `طلب الحساب` على البطاقة، يظهر تنبيه أوضح بدل أن يبقى مجرد وسم ثابت.
- تم توحيد عرض `طلب الحساب` في شريحة الطاولة مع زمنه، مع إبقاء منطق `no-order watch` أولوية عندما تكون الجلسة بلا طلبات أصلًا.
- تم توسيع `CashierTableStripBoard` ليعرض لكل جلسة نشطة:
  - عمر طلب الحساب
  - `minimumCharge`
  - `minimumGap`
  - مع إبراز بصري أقوى للجلسات المتأخرة في التسديد أو الناقصة عن الحد الأدنى.
- تم دعم هذه البيانات من الباك إند عبر `table-overview` بحيث لا يبقى احتسابها منطقًا متفرقًا في الواجهات.
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `api_server.py`
    - `WaiterTablesPage.tsx`
    - `CashierTableStripBoard.tsx`
  - `npm run build` نجح

21) تنفيذ جديد — شريحة مراجعة الكابتن قبل طلب الحساب:
- عند ضغط `طلب الحساب` من شاشة الكابتن لم يعد الطلب يُرسل مباشرة.
- أضيفت شريحة مراجعة جديدة داخل `WaiterOrderPage` تسبق اعتماد طلب الحساب وتعرض:
  - بنود الحساب الحالية
  - الإجمالي المتوقع
  - الخدمة والضريبة والتيبس
  - فرق `minimum charge` إن وجد
  - توزيع البنود على الكراسي
- من نفس الشريحة أصبح الكابتن يستطيع فتح `طلب مرتجع ضيف` قبل اعتماد الحساب، بحيث يبقى مسار المرتجع جزءًا من مراجعة الشيك لا خطوة منفصلة ذهنيًا.
- أضيف حفظ محلي لاسم الطابعة المعتمدة لهذه الشاشة عبر `localStorage` بمفتاح مستقل لشاشة الكابتن.
- بعد اعتماد الشريحة:
  - يتم تنفيذ `request bill` الفعلي
  - ثم تُفتح مباشرة طباعة الشيك على الفاتورة الحقيقية المنشأة
  - مع استمرار تسجيل `printCount` وسياسة أن الكابتن يملك طباعة واحدة فقط
- تم توسيع `CashierPayInvoiceModal` ليدعم:
  - عنوان مخصص
  - `autoPrintOnOpen`
  - عرض `printerHint`
  ليُستخدم أيضًا كشاشة `شيك الكابتن قبل التسديد` وليس للكاشير فقط.
- في حالة السبليت حسب المقاعد:
  - يُنشأ الطلب كالمعتاد
  - ويُفتح أول شيك مباشرة للطباعة
  - وتبقى بقية الشيكات متاحة من قائمة فواتير الجلسة للطباعة اليدوية.
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `CaptainBillReviewModal.tsx`
    - `CashierPayInvoiceModal.tsx`
    - `WaiterOrderPage.tsx`
  - `npm run build` نجح

22) تنفيذ جديد — نقل محطات الشيف المختص إلى تبويب المطبخ:
- تم نقل مدخل الصفحة الخاصة بتشغيل المطبخ/الشيف المختص من قسم `إعدادات الصالة` إلى قسم `إعدادات المطبخ والطباعة`.
- أضيف مسار أوضح باسم `kitchen-ops` داخل إعدادات المدير والمطور.
- أُبقي المسار القديم `restaurant-ops` كتحويل داخلي إلى `kitchen-ops` حتى لا تنكسر الروابط القديمة أو أي اعتماد سابق.
- التسمية الجديدة في القائمة:
  - `2.3 محطات الشيف المختص وتشغيل المطبخ`
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `SettingsLayout.tsx`
    - `App.tsx`
  - `npm run build` نجح

23) تنفيذ جديد — استعادة ظهور شرائح الطاولات بعد تعليق مسارات القراءة:
- تم تشخيص أن اختفاء الشرائح لم يكن من `WaiterTablesPage.tsx` نفسها، بل من تعليق endpoints الخلفية التي تغذيها:
  - `GET /api/restaurant/tables`
  - `GET /api/restaurant/operational-snapshot`
- السبب العملي كان أن بعض قراءات SQL المرتبطة بحالة المطعم والطاولات كانت قد تنتظر القفل أو الأعمال الجانبية داخل طلب العرض نفسه، فيبقى تحميل الشرائح معلقاً.
- تم تطبيق إصلاحين حاسمين في الباك إند:
  - إضافة `SET LOCK_TIMEOUT 3000` إلى قراءات `MAT3AM_RESTAURANT_STATE` في `_restaurant_sql_get` و `_restaurant_sql_get_bulk`
  - إضافة `SET LOCK_TIMEOUT 3000` إلى قراءة `TBL005` داخل `mat3am_sql_cache.py`
- كما تم فصل مزامنة تنبيهات `no-order watch` عن طلب عرض الطاولات:
  - `restaurant_get_tables` لم يعد ينتظر `_restaurant_sync_no_order_watch_alerts` بشكل متزامن
  - المزامنة أصبحت تعمل بالخلفية حتى لا تعطل ظهور الشرائح
- نتيجة التحقق بعد الإصلاح:
  - `GET /api/restaurant/operational-snapshot` عاد خلال أقل من ثانية تقريباً
  - `GET /api/restaurant/tables` عاد خلال `0.266s`
  - التحقق البصري عبر المتصفح أكد رجوع صفحة `شريحات الطاولات` وظهور البطاقات وأزرارها التشغيلية
  - `T15` ظهرت مجددًا ضمن البيانات الراجعة من API

24) تنفيذ جديد — إنهاء التسكين لا يبقي الطاولة محسوبة على الكابتن السابق:
- تم تشخيص خلل واجهة في `WaiterTablesPage.tsx`:
  - خريطة `sessionByTable` كانت تُبنى من كل جلسات اليوم
  - وليس من الجلسات النشطة فقط
- نتيجة ذلك:
  - بعد `إنهاء التسكين` أو `إرجاع الطاولة جاهزة` قد تبقى الطاولة مربوطة محلياً بـ `sessionId` قديم
  - فتعود الشريحة بلا اسم كابتن لكن يظل عندها أثر جلسة منتهية
- تم الإصلاح في نقطتين:
  - قصر `sessionByTable` على الجلسات ذات الحالة `active` فقط
  - إضافة تنظيف محلي فوري بعد `close/reset_ready` لإزالة:
    - الجلسة من `sessions`
    - الربط من `sessionByTable`
    - الطاولة من `busyIds`
    - الطاولة من `billReqIds`
    - مع تحديث حالة الشريحة إلى `dirty` أو `ready` بحسب الإجراء
- النتيجة المقصودة:
  - زر `إنهاء التسكين` يعيد الشريحة بلا كابتن
  - ولا تبقى محسوبة على الكابتن السابق بعد إغلاق الجلسة
- التحقق:
  - `GetDiagnostics` نظيف في `WaiterTablesPage.tsx`
  - `npm run build` نجح

25) تنفيذ جديد — اختصار فتح شاشة الطلب وتقليل التحميلات المكررة:
- تمت مراجعة مسار الزمن بين:
  - فتح `طلب للطاولة` حتى ظهور الأصناف
  - والرجوع من شاشة الطلب إلى `شريحات الطاولات`
- تم تثبيت أن `WaiterOrderPage.tsx` كانت تعتمد على `order-taker-bootstrap` ثم تعيد فورًا طلب بيانات مرتبطة بنفس الجلسات/الإعدادات، ما يضيف ضغطًا غير لازم في أول ثواني فتح الصفحة.
- تم تنفيذ اختصار آمن على مرحلتين:
  - استخدام الجلسات الراجعة من `order-taker-bootstrap` كمرجع فوري داخل الصفحة بدل إعادة طلب `table-sessions?status=active` مباشرة لحسم `activeSessionId` وبيانات الجلسة
  - تأخير استطلاع `workflow-settings` و`table-sessions` الدوري إلى ما بعد أول عرض بدل تشغيله فور الدخول
- تم أيضًا حذف التكرار الفوري لجلب:
  - `kitchen/item-stops`
  لأن `bootstrap` يعيدها أصلًا، وبقي التحديث الدوري فقط كل 15 ثانية
- تم توسيع `backend/api_server.py` بحيث يُرجع `workflowSettings` داخل `GET /api/restaurant/order-taker-bootstrap`، ليتحدد `orderTakerExclusiveTable` من نفس الحمولة الأولى دون طلب إضافي مباشر
- تم الإبقاء على طلبات التشغيل الثانوية الضرورية فقط بعد الفتح مثل:
  - `orders?sessionId=...`
  - `invoices-local?...`
  - وبيانات المنيو اليومية
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `src/pages/WaiterOrderPage.tsx`
    - `backend/api_server.py`
  - `npm run build` نجح
  - التحقق البصري عبر المتصفح أكد:
    - فتح شاشة الطلب وظهور الفئات والأصناف والطاولة المحددة
    - والرجوع إلى `شريحات الطاولات` عبر التنقل الطبيعي

26) تنفيذ جديد — سد الخلل المنطقي في بدء التسكين على الطاولة ذات الطلبات المفتوحة:
- بعد تشخيص `T12` ثبت أن النظام كان يمنع `إلغاء التسكين` عند وجود طلبات مفتوحة على الطاولة، لكنه لم يكن يمنع دائمًا `بدء تسكين جديد` على نفس الطاولة إذا لم توجد جلسة `active` مناسبة ظاهريًا.
- تم إصلاح المنبع في `backend/api_server.py`:
  - إضافة helper موحد لاكتشاف الطلبات المفتوحة الفعلية على الطاولة نفسها (`pending` / `preparing` / `ready`) بغض النظر عن الجلسة
  - استخدامه داخل `POST /api/restaurant/table-sessions` لمنع إنشاء جلسة جديدة على طاولة تحمل طلبات مفتوحة من جلسة أقدم
  - الإبقاء على نفس القاعدة داخل مسار `reset_ready` لكن عبر نفس helper الموحد بدل تكرار المنطق
- تم إصلاح واجهة الاستقبال في `src/pages/ReceptionPage.tsx`:
  - لم تعد تعتبر التسكين ناجحًا إذا رفضه الـ API
  - لم تعد ترقي حالة الطاولة إلى `occupied` محليًا عند فشل إنشاء الجلسة
  - باتت تعرض رسالة الرفض الفعلية القادمة من الخادم
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `backend/api_server.py`
    - `src/pages/ReceptionPage.tsx`
  - `npm run build` نجح
  - التحقق العملي أكد أن إجبار إنشاء جلسة جديدة على `T12` (`forceNewSession=true`) يعيد `409` بدل إنشاء جلسة إضافية

27) تنفيذ جديد — إضافة Reset للطاولة كتنظيف تشغيلي كامل:
- تم اعتماد تسمية جديدة واضحة للمدير/المطور في شريحة الطاولة: `Reset للطاولة`.
- هذا الإجراء ليس مجرد `إلغاء التسكين` الآمن، بل تنظيف كامل للطاولة من بقايا التشغيل:
  - إلغاء الطلبات المفتوحة على نفس الطاولة وتحويل سطورها إلى `cancelled`
  - إنهاء كل الجلسات النشطة على الطاولة نفسها مع فك ارتباط الكابتن
  - إعادة سجل الطاولة إلى `ready` مع تصفير مؤشرات الانشغال و`no-order watch`
- التنفيذ في `backend/api_server.py`:
  - إضافة helper مركزي `_restaurant_force_reset_table(...)`
  - إضافة action جديد `reset_table` داخل endpoint `/api/restaurant/table-sessions/{session_id}/no-order-watch`
  - تقييد `Reset للطاولة` على أدوار `manager` و`developer` فقط
- التنفيذ في `src/pages/WaiterTablesPage.tsx`:
  - الإبقاء على `إلغاء التسكين` كمسار آمن قائم
  - وإضافة زر مستقل باسم `Reset للطاولة` للمدير/المطور فقط مع رسالة نجاح واضحة بأنه تنظيف كامل للطاولة
- التحقق:
  - `GetDiagnostics` نظيف في:
    - `backend/api_server.py`
    - `src/pages/WaiterTablesPage.tsx`
  - `npm run build` نجح بعد إضافة الإجراء الجديد

28) تنفيذ جديد — Reset للطاولة صار يعمل على مستوى table_id:
- بعد تشخيص حالة مثل `T10` ثبت أن المشكلة قد تبقى موجودة حتى عندما لا توجد `session` نشطة ظاهرة، لأن الطاولة قد تحمل طلبات orphan مفتوحة مرتبطة بجلسة أقدم.
- تم اعتماد endpoint مباشر في `backend/api_server.py`:
  - `POST /api/restaurant/tables/{table_id}/reset`
  - بصلاحية `manager / developer` فقط
  - ويستدعي helper المركزي `_restaurant_force_reset_table(...)`
- تم تعديل `src/pages/WaiterTablesPage.tsx` بحيث:
  - لم يعد زر `Reset للطاولة` يعتمد على `sessionId`
  - ولم يعد محصورًا داخل `no-order watch`
  - بل صار يظهر على بطاقة الطاولة نفسها للمدير/المطور عندما تكون الطاولة:
    - `occupied`
    - أو عليها جلسة نشطة
    - أو عليها طلبات/مؤشرات تشغيلية تجعل التنظيف الكامل منطقيًا
- النتيجة المقصودة:
  - الطاولات العالقة مثل `T10` يمكن تنظيفها مباشرة من الشريحة حتى لو اختفت الجلسة النشطة وبقيت الطلبات المفتوحة القديمة
  - ويبقى `إلغاء التسكين` مسارًا آمنًا منفصلًا لحالة الجلسة بلا طلبات

29) تنفيذ جديد — اعتماد هوية SIR RESTO في الواجهة و EXE 028:
- تم اعتماد الصورة:
  - `E:\XTRA_WEB\مطاعم\SIRRESTO .jpeg`
  كمصدر هوية جديد للتطبيق.
- تم تحويل الصورة إلى:
  - `public/app-logo.png`
  ثم مزامنتها أيضًا إلى:
  - `ui/restaurant/app-logo.png`
  حتى تظهر في شاشة التحميل ورمز الموقع وصورة التطبيق داخل الواجهة.
- تم تعديل `scripts/prepare_mat3am_exe_build.py` ليعطي أولوية لصورة `SIRRESTO .jpeg` عند توليد:
  - `assets/mat3am_icon.ico`
  بدل الاعتماد الحصري على الصورة القديمة.
- تم تعديل `src/components/AppShell.tsx` و `src/styles/appShell.css` لإظهار اللوجو بشكل ثابت وخفيف:
  - داخل رأس القائمة الجانبية
  - وكشارة ثابتة صغيرة على الصفحات دون تعطيل التفاعل
- بسبب استمرار قفل `ui/restaurant/index.html` في البيئة الحالية، تم بناء الواجهة إلى:
  - `_pack_ui/restaurant`
  ثم تغليف EXE باستخدام:
  - `_Mat3amPOS_pack.spec`
- تم إنشاء النسخة الجديدة بنجاح:
  - `dist/Mat3amPOS028.exe`
- طابع البناء الحالي:
  - `2026-06-01T20:49:12Z code=M260601-2049-1665E07 git=1665e07`

30) تنفيذ جديد — Launcher مباشر لتثبيت ODBC وتشغيل المُثبت:
- تم إضافة ملف تشغيل واضح في جذر المشروع:
  - `run_setup_odbc_and_install.bat`
- وظيفة هذا الملف:
  - التحقق من وجود `setup_odbc_and_install.bat`
  - إعادة تشغيله بصلاحية Administrator تلقائيًا عند الحاجة
  - أو استدعاؤه مباشرة إذا كانت الجلسة مرفوعة الصلاحية بالفعل
- الهدف:
  - إعطاء المستخدم مدخلًا واضحًا وآمنًا لتشغيل تثبيت ODBC + تثبيت البرنامج دون الحاجة لكتابة أوامر يدويًا

31) تنفيذ جديد — Setup EXE مستقل للتحقق من ODBC وتشغيل البرنامج:
- تم إنشاء launcher جديد قابل للتغليف في:
  - `scripts/mat3am_setup_launcher.py`
- وظيفة هذا الـ launcher:
  - التحقق من وجود `ODBC Driver 17/18 for SQL Server`
  - طلب صلاحية Administrator تلقائيًا عند الحاجة
  - تحميل `ODBC Driver 18` من Microsoft وتثبيته إذا لم يكن موجودًا
  - تشغيل `Mat3amPOS.exe` بعد نجاح التحقق/التثبيت
  - استخدام fallback إلى `setup_odbc_and_install.bat` عند فشل المسار الأصلي
- تم تعديل `START_HERE.bat` بحيث:
  - يفضّل تشغيل `Mat3amPOS_Setup.exe` إذا كان موجودًا
  - ثم fallback إلى `run_setup_odbc_and_install.bat`
  - ثم fallback أخير إلى المنطق القديم داخل BAT نفسه
- تم إضافة build محلي مستقل:
  - `build_setup_launcher.bat`
  - وينتج:
    - `dist/Mat3amPOS_Setup.exe`
- تم تحديث `.github/workflows/build-exe.yml` لضم:
  - بناء `Mat3amPOS_Setup.exe`
  - وإرفاقه داخل package النهائي مع ملفات التشغيل المساندة
